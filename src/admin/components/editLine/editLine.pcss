.edit-line-component {
  &.blocked {
    opacity: 0.5;
    filter: grayscale(100%);
    pointer-events: none;
  }
}

.title {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.input {
  width: 80%;
}
.icons{
  width: 10.3%;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.buttons {
  display: flex;
  padding: 1px;
}

.button-icon {
  margin-right: 20px;

  &:last-child {
    margin-right: 0;
  }
}